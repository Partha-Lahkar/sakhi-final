// gemini_chatbot_screen.dart

import 'package:flutter/material.dart';

class GeminiChatbotScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gemini Chatbot'),
      ),
      body: Center(
        child: Text('This is the Gemini Chatbot screen.'),
      ),
    );
  }
}
